# Minimally Completed Plans

Plans whose feature shipped well enough that the original plan is no longer
load-bearing for ongoing work, but which we keep around for archaeology when
something needs to be re-evaluated.

These are NOT abandoned plans (they delivered) and NOT actively-tracked plans
(they're done). Move work back to `../plans/` if a plan is reopened.

## Index

| Date       | Plan | Notes |
|------------|------|-------|
| 2026-04-24 | hitbox-visualizer | Plan + design spec. Hitbox toggles ship in `practice_hitbox.c`; visualization is good-enough on hardware. |
