#ifndef PRACTICE_HEAP_AUDIT_H
#define PRACTICE_HEAP_AUDIT_H

/* Telemetry for Phase 4 heap audit (plan §7). Built when PRACTICE_ROM=1.
 * PRACTICE_HEAP_AUDIT is set by the Makefile (default 1); define 0 to
 * compile out PerFrame work and Boot logging. */

#endif
